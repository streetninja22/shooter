#pragma once

#include <framework/InputSystem.h>

namespace keybind
{
	using namespace inpt;

	const KeyCode MOVE_UP		 = KEYCODE_ARROW_UP;
	const KeyCode MOVE_DOWN		 = KEYCODE_ARROW_DOWN;
	const KeyCode MOVE_LEFT		 = KEYCODE_ARROW_LEFT;
	const KeyCode MOVE_RIGHT	 = KEYCODE_ARROW_RIGHT;

	const KeyCode SHOOT			 = KEYCODE_Z;
	const KeyCode BOMB			 = KEYCODE_X;

	const KeyCode PAUSE			 = KEYCODE_SPACE;
}